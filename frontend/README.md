# Frontend (React + TypeScript)

This folder will contain the React + TypeScript application for the document assistant UI.

Suggested structure:
- src/
- public/
- package.json

Run locally:
- npm install
- npm start